# Optional TOML config path. Default is config.toml.
CONFIG_PATH=config.toml

# Environment variables override matching TOML values when set.
# RUST_LOG=debug,ort=warn
# MAX_NEW_TOKENS=256
# EXECUTION_PROVIDERS=auto
# MODEL_PATH=Florence-2-base/onnx/vision_encoder.onnx
